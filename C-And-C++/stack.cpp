#include <iostream>
us


using namespace std;
int main()
{
    stack<int> s;
    s.push(2);
    s.push(3);
    s.pop();
    cout<<"printing the top element"; s.top();
    if(s.empty ()){
        cout<<"stack is empty";
        }
        else{
            cout<<"stack is not empty";
            cout <<"size of the stack is "<< s.size();{
                return 0;
            }
        }
    }




